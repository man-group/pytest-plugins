# $HeadURL$
""" Sample module for command-line app.
"""
import sys
from optparse import OptionParser


def main():
    """ This is the main run-method for this command-line application.
    """
    usage = "usage: %s [options] args " % sys.executable
    parser = OptionParser(usage=usage)

    (options, args) = parser.parse_args()
    # call your own code from here with options and/or args


if __name__ == '__main__':
    main()