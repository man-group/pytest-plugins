dependencies:
  pre: 
#    - source /etc/lsb-release && echo "deb http://download.rethinkdb.com/apt $DISTRIB_CODENAME main" | sudo tee /etc/apt/sources.list.d/rethinkdb.list
#    - wget -qO- https://download.rethinkdb.com/apt/pubkey.gpg | sudo apt-key add -
#    - sudo apt-get update
#    - sudo apt-get install -y apache2 rethinkdb jenkins

test:
  override:
    - make test 
    - for i in pytest-*; do cp $i/junit.xml $CIRCLE_TEST_REPORTS/$i.xml || true; done
    - venv/bin/coverage combine pytest-*/.coverage 
    - venv/bin/coverage html -d $CIRCLE_ARTIFACTS
     
machine:
  services :
     - mongodb
     - redis
  environment : 
     SERVER_FIXTURES_RETHINK : 
     SERVER_FIXTURES_JENKINS_WAR : 
     SERVER_FIXTURES_HTTPD : 
     SERVER_FIXTURES_MONGO_BIN : 
     SERVER_FIXTURES_JAVA : 
     SERVER_FIXTURES_XVFB : 
     SERVER_FIXTURES_REDIS : 
      
